import { Link } from "react-router-dom";

function FalkButton() {
    return (
        <Link to="/" className="text-[30px] font-semibold text-primary-color">Falk</Link>
    )
}

export default FalkButton